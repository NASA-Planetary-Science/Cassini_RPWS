# This is a reasonable default if you want to run out of the source directory,
# but the "make install" step rewrites this value below to the cassini ETC
# directory.   Which is typically /opt/project/cassini/etc
g_sConfDir = "/opt/project/cassini/etc"

g_nSpiceId = -82

from moonEncounter import *
from tables import *

