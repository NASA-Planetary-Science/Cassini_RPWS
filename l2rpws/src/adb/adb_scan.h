 /*
  *     Returns with adb structure filled in
  *       with a new filename.
  *
  *     returns 1 if filename in adb
  *     returns 0 if no more files in database
  */
int ADB_scan (struct ADB *adb);
