 /*
  *     Returns with mdb structure filled in
  *       with a new filename.
  *
  *     returns 1 if filename in mdb
  *     returns 0 if no more files in database
  */
int MDB_scan (struct MDB *mdb);
